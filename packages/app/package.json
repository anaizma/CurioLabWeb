{
  "name": "@curiolab/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CurioLab platform application-service layer: the framework-agnostic ApplicationService for the intake and ops review flow (POST /public/apply and /ops/applications).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@curiolab/core": "*",
    "@curiolab/db": "*",
    "@curiolab/runtime": "*",
    "postgres": "^3.4.9",
    "resend": "^6.17.2"
  },
  "devDependencies": {
    "@types/node": "^20",
    "embedded-postgres": "17.10.0-beta.17",
    "eslint": "^9",
    "typescript": "^5",
    "typescript-eslint": "^8",
    "vitest": "^3"
  }
}
