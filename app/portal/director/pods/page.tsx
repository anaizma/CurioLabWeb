import DirectorStub from "@/components/portal/DirectorStub";
export default function Page() {
  return <DirectorStub title="Pods & terms" note="Create terms and pods and assign members. Wires to GET /api/ops/pods and /api/ops/terms when available." />;
}
