import DirectorStub from "@/components/portal/DirectorStub";
export default function Page() {
  return <DirectorStub title="Members" note="Chapter roster — activate pending memberships and view tiers and pods. Wires to GET /api/ops/memberships when available." />;
}
