import DirectorStub from "@/components/portal/DirectorStub";
export default function Page() {
  return <DirectorStub title="Deletion & export requests" note="Review and fulfill guardian data requests. Wires to GET /api/ops/deletion-requests and /api/ops/export-requests when available." />;
}
