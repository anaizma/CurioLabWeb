import DirectorStub from "@/components/portal/DirectorStub";
export default function Page() {
  return <DirectorStub title="Enrollments" note="Signed enrollment records for accepted applicants. Wires to GET /api/ops/enrollments when available." />;
}
