import DirectorStub from "@/components/portal/DirectorStub";
export default function Page() {
  return <DirectorStub title="Applications" note="Review and advance applications through screen → interview → accept/decline. Wires to the chapter's application list when GET /api/ops/applications is available." />;
}
